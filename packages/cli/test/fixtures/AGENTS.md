# Agent Context

This is the fixture AGENTS.md used by e2e tests.
It replaces the former artifacts/shared/AGENTS.md which no longer exists.

## Purpose

Provides a minimal context document for the install → check e2e flow.
